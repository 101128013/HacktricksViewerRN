// This file was moved to `test-utils/renderWithProviders.tsx` at project root.
// Kept for compatibility briefly but intentionally left empty so it's not executed as a test.
import renderWithProviders from '../../test-utils/renderWithProviders';

describe('renderWithProviders helper', () => {
	it('is a function', () => {
		expect(typeof renderWithProviders).toBe('function');
	});
});
